# Frontend — Relocation & Routine Copilot

Next.js (App Router) + TypeScript + Tailwind CSS.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:3000 — you should see the Milestone 0 placeholder page.

## Lint / typecheck

```bash
npm run lint
npm run typecheck
```
